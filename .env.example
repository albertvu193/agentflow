# AgentFlow Environment Configuration
# Copy this to .env and modify as needed

# Backend server port
PORT=3001

# Frontend dev server port (Vite)
VITE_PORT=5173

# Default Claude model for agents (sonnet, opus, haiku)
DEFAULT_MODEL=sonnet

# Data storage directory
DATA_DIR=~/.agent-viz
